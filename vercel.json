{
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "buildCommand": "prisma generate && node scripts/set-build-version.js && next build",
  "installCommand": "npm install"
}

